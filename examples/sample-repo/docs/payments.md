# Payments

Payments are retried 3 times before the request is failed.

> Note: this document is intentionally stale — the code actually uses 5 retries,
> so `truth check "we retry payments 3 times"` should be contradicted.
