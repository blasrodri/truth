# Intentionally empty fixture.
# Narrative-prose precision cases point here so diff-native claims have a
# real but EMPTY repo context (no git -> no diff -> inconclusive, never
# contradicted). Do NOT add code here.
