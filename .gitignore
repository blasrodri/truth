/target
**/*.rs.bk
# Cargo.lock IS committed: truth ships binaries, so reproducible builds matter
# (and Docker/Glama builds use `--locked`).

# truth runtime artifacts
.truth/
truth.toml
*.sqlite
*.sqlite-journal
